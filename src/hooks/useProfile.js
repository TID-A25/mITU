/**
 * React hook for fetching a single user profile by ID.
 * - Uses useEffect to fetch on mount or ID change
 * - Uses useState to track profile data
 * - Depends on service functions for DB access
 */