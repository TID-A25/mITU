/**
 * Implement all Parse Server API operations (CRUD) for user profiles.
 * - Expose functions like:
 *   - fetchProfiles()
 *   - fetchProfileById(id)
 *   - createProfile(data)
 *   - updateProfile(id, updates)
 *   - deleteProfile(id)
 * - Use async/await and Parse SDK calls (e.g., Parse.Object.extend).
 * - Handle errors via try/catch and propagate them to the caller.
 *
 * Each function should return raw Parse objects (or pass them to profileMapper).
 */