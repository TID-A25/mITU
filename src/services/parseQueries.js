/**
 * Centralized API functions for interacting with Parse Server.
 * - Handles CREATE, READ, UPDATE, DELETE (CRUD) operations
 * - Only business logic that talks directly to Parse should live here
 * - Used by Hooks to abstract backend logic from components
 */