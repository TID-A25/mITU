/**
 * Transforms raw Parse objects into plain JavaScript objects.
 * - Keeps your frontend logic clean and detached from Parse's data structure
 * - Converts field names, handles default values, formatting, etc.
 * - Called inside Services or Hooks before passing data to UI
 */